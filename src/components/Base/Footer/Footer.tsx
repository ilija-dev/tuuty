import { FooterContainer, Inner } from './Footer.style';

export const Footer = (): JSX.Element => {
    return (
        <FooterContainer>
            <Inner>
               <p>MADE WITH ❤️ BY TEAM ND6ME</p>
            </Inner>
        </FooterContainer>
    );
}